<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <header class="flex justify-between px-12 items-center bg-blue-100">
      <a href="/" class="font-serif font-semibold text-xl">YelpCamp</a>
      <section
        class="flex justify-between items-center gap-6 text-lg font-semibold py-6"
      >
        <a href="/" class="hover:underline">Login</a>
        <a href="/" class="hover:underline">Sign Up</a>
        <a href="/" class="hover:underline">Logout</a>
      </section>
    </header>
    <section class="flex flex-col justify-center items-center gap-12 py-20">
      <h1 class="text-center font-semibold text-6xl">Add New Comment</h1>
      <form
        class="flex flex-col w-1/3 gap-8"
        action="/camps/<%= camp._id %>/comments/"
        method="POST"
      >
        <input
          class="border px-8 py-2 rounded-lg"
          type="text"
          name="comment[author]"
          placeholder="Author"
        />
        <input
          class="border px-8 py-2 rounded-lg"
          type="text"
          name="comment[text]"
          placeholder="comment"
        />
        <button
          class="px-8 py-2 rounded-lg bg-blue-600 text-white font-semibold"
        >
          Add a comment
        </button>
      </form>
      <a
        href="/camps/<%= camp._id %>"
        class="hover:underline bg-blue-300 rounded-md px-4 py-1"
        >Go Back</a
      >
    </section>
  </body>
</html>
